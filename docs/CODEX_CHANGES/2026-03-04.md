# 2026-03-04

## Hecho

- Refactor completo de `prediction` para dejar de depender del debug `v12`.
- Nuevo agregador en `core/prediction_lab.py` con 4 bloques explícitos:
  - `data_inventory`
  - `prior_model`
  - `intraday_model`
  - `market_model`
- El endpoint `/api/prediction/{station_id}` ahora:
  - resuelve horizonte `today / tomorrow / +2 days`
  - usa `external_sources` para el prior
  - usa `world` + `PWS` + `trading_signal` para la capa intradía
  - incorpora resumen histórico PWS sin pasar por HTTP
  - inspecciona el canal `features` para dejar visibles carencias reales
  - mantiene `legacy_reference` sólo como comparación
- Reemplazo completo de `templates/prediction.html` por una UI nueva orientada a probabilidades y lectura operativa:
  - selector de horizonte
  - inventario de datos disponible / faltante / por estudiar
  - curvas target-day por proveedor
  - comparación modelo vs mercado
  - capa same-day oficial + PWS
  - ranking histórico PWS
  - backlog de investigación

## Decisiones

- El histórico PWS en la vista live usa ventana móvil de hasta `14` días por defecto para no convertir la página en una operación demasiado pesada cuando crezca el histórico.
- La falta de snapshots históricos de forecast no se oculta: aparece como gap explícito en `inventory` y `research_backlog`.
- `features.env` vacío también se expone como gap real del modelo, no como dato implícitamente disponible.

## Tests

- `tests/test_prediction_lab.py`
- `tests/test_web_server_prediction_api.py`
- `tests/test_prediction_template.py`

Resultado:

- `2 passed`
- `2 passed`
- `1 passed`

## Pendiente natural

- Persistir snapshots horarios de forecast para medir drift run-to-run antes del settlement.
- Enriquecer `features` con viento, nubosidad, humedad, precipitación y solar.
- Extender el estudio PWS con métricas de convergencia temporal, no sólo hit/miss agregado.
