# 2026-03-03 - Revision de cambios de Claude y fixes de Codex

## Contexto

Se revisaron los documentos mas recientes en `docs/CLAUDE_CHANGES` y el codigo asociado.

Restriccion aplicada:

- `docs/CLAUDE_CHANGES` se mantiene como log de Claude
- las observaciones y cambios de Codex se documentan aqui

## Opinion sobre los cambios revisados

### `2026-03-03.md` - ensemble counting

Direccion correcta, pero habia dos fallos funcionales importantes:

1. el ensemble no entendia bien labels reales de Polymarket en Celsius ni buckets terminales
2. `micro_value_ensemble` podia reabrir la misma posicion porque el anti-duplicado comprobaba la clave equivocada

Ademas, el test de equity base de `papertrader` no era "pre-existente": el fallo venia de haber pasado de 4 a 5 estrategias sin actualizar el test.

### `2026-03-03_detail_view.md` - vista detalle

Cambio util y de bajo riesgo.

No lo toque en codigo. Solo queda como sugerencia futura revisar si el endpoint de detalle deberia paginar eventos en vez de hacer tail fijo.

### `2026-03-03_price_sanity.md` - sanity check de precios NO

El guardrail me parece razonable como defensa operativa.

Tampoco lo toque en esta pasada. La idea es valida, aunque sigue siendo heuristica y no sustituye una validacion mas completa de microestructura.

## Cambios hechos por Codex

### `collector/ensemble_fetcher.py`

Se corrigio el mapping del ensemble a labels de mercado:

- labels Celsius unitarios ahora salen como `11°C`, `12°C`, etc., no `11-11°C`
- `EnsembleSnapshot.get_bucket_prob()` ahora entiende semantica de labels de Polymarket:
  - `x°C`
  - `x-y°C`
  - `x°F or below`
  - `x°F or higher`
  - equivalentes en Celsius
- el calculo usa redondeo `half-up`, alineado con la semantica esperada de settlement
- se normalizan labels de entrada antes de comparar
- se corrigio el alineado del ultimo bucket Fahrenheit para no generar el extremo desplazado

Impacto:

- `fetch_ensemble_edge()` ya puede valorar tails y mercados Celsius con labels reales
- la API `/api/ensemble/{station_id}` devuelve labels mas legibles en mercados Celsius

### `core/papertrader.py`

Se arreglo el anti-duplicado de `micro_value_ensemble`:

- la lane ahora consulta `strategy_state["positions"]` usando `base_key`
- antes buscaba `full_key`, que no coincide con la estructura real del mapa local de posiciones
- `load_papertrader_config()` ahora hereda `shared.bankroll_usd` tambien como `initial_bankroll_usd`

Impacto:

- si una posicion de esa lane ya esta abierta, no vuelve a proponerse en ticks sucesivos por error de clave
- el bankroll inicial por estrategia deja de quedarse fijado en `100` cuando el entorno ya estaba en `50`

### `web_server.py`

Se corrigio el reset del papertrader:

- `reset_papertrader_session()` ya no fuerza `100.0`
- ahora resuelve el bankroll configurado, lo persiste en runtime y lo aplica explicitamente al `PaperTrader` antes de reconstruir la sesion
- los overrides legacy de `data/papertrader_runtime.json` ya no pueden pisar el bankroll base del entorno salvo que vengan marcados como override explicito de runtime (`initial_bankroll_source = "runtime"`)
- cuando la UI guarda un bankroll manual, `update_papertrader_config_api()` marca ese valor como override explicito de runtime

Impacto:

- el reset limpio respeta el bankroll actual deseado por lane
- evita que un estado antiguo con `100` contamine un reinicio que deberia volver a `50`
- evita el caso visto en la VM: `.env` en `50`, pero `papertrader_runtime.json` legacy en `100`, provocando que `Reset` siguiera reseteando a `100`

### `tests/test_papertrader.py`

Se actualizaron y reforzaron tests:

- expectativas agregadas de equity adaptadas a `len(PAPER_STRATEGY_ORDER)`
- snapshot inicial adaptado a 5 estrategias
- nuevo regression test para que `micro_value_ensemble` no reabra una posicion ya abierta

### `tests/test_ensemble_fetcher.py`

Nuevo archivo de tests para cubrir:

- labels unitarios en Celsius
- lookup por rango en Celsius
- lookup de buckets terminales en Fahrenheit

### `tests/test_web_server_ensemble_api.py`

Nuevo test end-to-end del endpoint `/api/ensemble/edge/{station_id}` para cubrir:

- extraccion de precios desde `best_ask`, `price` y `yes_price`
- filtro de buckets vacios o con precio cero
- parseo de `target_date`
- propagacion de `min_edge`
- respuesta `404` cuando no hay mercado disponible

### `tests/test_web_server_papertrader_api.py`

Nuevos tests para validar:

- que `reset_papertrader_session()` usa el bankroll resuelto y no vuelve a `100` por hardcode
- que `_build_papertrader_config()` ignora overrides legacy de bankroll sin marca explicita
- que un override explicito de runtime si sigue respetandose
- que `update_papertrader_config_api()` marca el bankroll guardado desde UI como override explicito

### `templates/polymarket.html`

Se corrigio el flujo activo de la UI del papertrader:

- la segunda definicion de `syncPapertraderDraft()` ya no reinyecta `initial_bankroll_usd = 100`
- la segunda definicion de `savePapertraderConfig()` ya no envia `100` hardcodeado al backend
- el panel vuelve a exponer el input `pt-bankroll` en la vista activa
- `bindPapertraderDraftEvents()` vuelve a escuchar cambios en `pt-bankroll`
- los textos de hero, resumen y reset pasan a usar bankroll y numero de estrategias dinamicos en vez de `4` y `$100` fijos

Impacto:

- al pulsar `Start` despues de un `Reset`, la UI ya no pisa el bankroll configurado con `100`
- el valor de `50` por estrategia sobrevive al flujo normal de guardar, resetear y arrancar desde la web
- la card de cada estrategia deja de presentar `price_anomaly` como si fueran compras o ventas ejecutadas
- los eventos `price_anomaly` ahora muestran detalle util (`ask`, `fair`, `signal`) cuando esta disponible, y la seccion pasa a llamarse `Actividad reciente`

### `core/papertrader.py` - ruido de `price_anomaly`

Se redujo el ruido operativo del journal:

- `price_anomaly` ahora tiene dedupe por clave de evento durante 90 segundos
- la dedupe actua sobre el journal y sobre `recent_events`, evitando que la misma lane llene la card con el mismo warning en cada tick

Impacto:

- desaparecen secuencias como 4 alertas identicas consecutivas para la misma lane/mercado
- el usuario sigue viendo la anomalia, pero no repetida en cada iteracion del loop

### `tests/test_polymarket_template_papertrader.py`

Nuevo test textual de regresion para asegurar que la plantilla activa:

- no vuelve a hardcodear `initial_bankroll_usd: 100`
- no vuelve a mostrar mensajes de reset fijos a `$100`
- sigue exponiendo `pt-bankroll`
- sigue enviando `ptDraft.initial_bankroll_usd` al backend
- renderiza `price_anomaly` como actividad con detalle legible

### `.env` local y remoto

Revision operativa de configuracion sin exponer secretos:

- autotrade sigue deshabilitado (`HELIOS_AUTOTRADE_ENABLED=false`)
- Wunderground, Telegram, recorder y compactor estan habilitados
- siguen coexistiendo credenciales legacy `POLYMARKET_*` y nuevas `HELIOS_POLY_*`

Sincronizacion remota realizada:

- destino: `/opt/helios/.env` en la VM documentada de Dublin
- backup remoto creado antes de sobrescribir: `/opt/helios/.env.backup.20260303T101713Z`
- hash local y remoto final alineado tras la copia
- posteriormente si se reinicio `helios.service`

### Despliegue papertrader en VM

Aplicado en remoto:

- backups de `core/papertrader.py`, `web_server.py`, `data/papertrader_runtime.json` y `data/papertrader_state.json`
- despliegue de los fixes de bankroll y reset
- correccion del `papertrader_runtime.json` remoto a UTF-8 sin BOM
- reset limpio del papertrader y nuevo arranque

Correccion posterior del bug residual de bankroll:

- verificado en la VM que `/opt/helios/.env` ya tenia `HELIOS_AUTOTRADE_BANKROLL_USD=50`
- detectado que el valor que seguia pisando era `/opt/helios/data/papertrader_runtime.json` con `initial_bankroll_usd = 100.0`
- detectado ademas que `/opt/helios/data/papertrader_state.json` seguia en estado limpio pero con `100.0` por lane
- copiado el `web_server.py` corregido a `/opt/helios/web_server.py` sin reiniciar el servicio
- normalizado `/opt/helios/data/papertrader_runtime.json` a `initial_bankroll_usd = 50.0`
- normalizado `/opt/helios/data/papertrader_state.json` a `50.0` por lane y `250.0` agregado

Nota operativa:

- `helios.service` no se ha reiniciado en esta pasada
- el proceso en memoria puede seguir sirviendo el codigo viejo hasta que lo reinicies tu
- aun asi, los ficheros persistidos ya quedan preparados para que el siguiente `Reset`/reinicio arranque en `50`
- verificacion final por API remota: `/api/papertrader/status` ya devuelve `initial_bankroll_usd = 50.0`, `aggregate_initial_bankroll_usd = 250.0` y todas las lanes a `50.0`

Estado final verificado en la VM:

- `station_ids = ["KLGA","KATL","KORD","KMIA","LFPG","EGLC"]`
- `initial_bankroll_usd = 50.0`
- `aggregate_initial_bankroll_usd = 250.0`
- `max_total_exposure_usd = 4.0`
- `running = true`

## Verificacion

Ejecutado con `PYTHONPATH=.`:

- `pytest tests/test_ensemble_fetcher.py -q`
- `pytest tests/test_papertrader.py -q`
- `pytest tests/test_web_server_ensemble_api.py -q`
- `pytest tests/test_web_server_papertrader_api.py -q`
- `pytest tests/test_polymarket_template_papertrader.py -q`

Resultado:

- `2 passed`
- `37 passed`
- `2 passed` (endpoint `/api/ensemble/edge/{station_id}`)
- `5 passed` (runtime + reset bankroll del papertrader)
- `2 passed` (regresiones de plantilla activa)

Warnings observados:

- FastAPI marca `@app.on_event("startup")` y `@app.on_event("shutdown")` como deprecados
- `core/atenea/chat.py` sigue emitiendo `FutureWarning` por `google.generativeai`
- Open-Meteo sigue devolviendo `429` en algunos fetches de fondo; no bloquea el arranque pero si puede degradar señales auxiliares

Validacion adicional con snapshot real:

- `fetch_ensemble_snapshot('EGLC')` devuelve labels tipo `11°C`
- `fetch_ensemble_snapshot('KLGA')` mantiene labels tipo `36-37°F`
- lookup terminal tipo `36 F or below` devuelve probabilidad valida

## Sugerencias no implementadas

1. revisar si `fetch_ensemble_edge()` debe devolver `BUY_NO` en vez de `SELL` cuando un bucket no aparece en la distribucion
2. revisar a futuro el threshold del `price_sanity` de NO (`50%`) con datos reales del CLOB
3. paginar la vista detalle de estrategia si el journal supera ampliamente el tail actual
